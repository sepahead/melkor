# Takeover audit — 2026-07-15

This is a point-in-time acceptance and CI audit of commit
`090126e921ae5ceff6df07ba2ddbc538f685d413`. The project author remains **Sepehr
Mahmoudian**. The implementation specification is
`/Users/torusprime/Downloads/melkor_production_v2_blueprint.md`.

## Strict blueprint acceptance

The blueprint defines 35 planned PRs. Applying each PR's full acceptance criteria, rather than
crediting a code slice as a complete PR, produced this count:

| Scope | Complete | Partial | Open |
|---|---:|---:|---:|
| PR01–PR34 | 0 | 21 | 13 |
| PR35 final release commit | 0 | 0 | 1 |
| **Total** | **0** | **21** | **14** |

`Partial` means relevant work exists but at least one required acceptance item or its evidence is
missing. None of the 35 PRs is acceptance-complete. This PR-level result is deliberately separate
from finding-level `closed`/`in-progress`/`open` dispositions in
[`production-blockers.md`](production-blockers.md).

## Exact-HEAD CI evidence

[CI run 29412275588](https://github.com/sepahead/melkor/actions/runs/29412275588) for `090126e`
failed on 2026-07-15. The failing jobs were:

- [`build-linux-cuda` job 87341931529](https://github.com/sepahead/melkor/actions/runs/29412275588/job/87341931529): link failure with an undefined `__cudaRegisterLinkedBinary_*` symbol.
- [`Python and shell checks` job 87341931534](https://github.com/sepahead/melkor/actions/runs/29412275588/job/87341931534): Ruff `F841` for the unused local `Version` in `tests/test_tools.py`.
- [`Tauri / Rust 1.88` job 87341931643](https://github.com/sepahead/melkor/actions/runs/29412275588/job/87341931643): regenerated `RUST_THIRD_PARTY_LICENSES.html` named `melkor-viewer 2.0.0-dev`, while the committed inventory still named `2.0.0-rc.1`.
- [`Fuzz smoke (libFuzzer)` job 87341931680](https://github.com/sepahead/melkor/actions/runs/29412275588/job/87341931680): the claimed committed seed corpus was absent; `fuzz/corpus/ply` did not exist in the checkout.
- [`CI Gate` job 87343354516](https://github.com/sepahead/melkor/actions/runs/29412275588/job/87343354516): correctly failed because the four required jobs above failed.

A pristine local build of the same commit passed **33/33** registered CTest targets. That is useful
native-test evidence, but it neither covers the failing CI-only checks nor makes the exact HEAD CI
green. Fixes require a new exact-commit CI run before any full-suite or release-gate claim.

## Provenance exception retained

The maintainer's 2026-07-15 deletion of the `v1.2.0` and `v1.2.1` tags and GitHub releases remains
an explicit, informed override of the blueprint's preserve-every-tag rule. It is not rewritten as
ordinary tag policy or silently removed; the full decision record remains in
[`v2-review-baseline.md`](v2-review-baseline.md) and [`v2-progress.md`](v2-progress.md). The
underlying commits remain in history, and the planned Zenodo DOI belongs to the future `v2.0.0`
release.
